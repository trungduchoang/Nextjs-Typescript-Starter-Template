// types
import { ROUTES } from "@/types/common";
// others
import CONSTANTS from "@/constants";

const routes: ROUTES = [
  {
    pageName: CONSTANTS.PAGE_NAMES.HOME,
    path: CONSTANTS.ROUTES.HOME,
    pages: "Home",
  },
];

export default routes;
